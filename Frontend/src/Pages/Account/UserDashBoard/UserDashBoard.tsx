interface Changeaccount {
    setIfaccount: (arg0: boolean) => void; // Define the type for the setChange prop
  }
const UserDashBoard = ({setIfaccount}:Changeaccount) => {
    return (
        <></>
    );
}
export default UserDashBoard ;